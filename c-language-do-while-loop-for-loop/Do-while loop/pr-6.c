#include<stdio.h>
main()
{ 
	int i,j=20,ans;
	printf("enter starting no:");
	scanf("%d",&i);
	do{ 
		if(n%2==0){ 
		printf("%d ",i); 
		ans = ans + i;
		} 
		i++; 
	} while(i<=j); 
	printf("\n\n sum of even no %d",ans); }
